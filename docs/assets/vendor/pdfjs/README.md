Place PDF.js files here to enable the local (0 external requests) PDF reader:

- `pdf.min.js`
- `pdf.worker.min.js`

Recommended source: download the matching `pdf.js` release build and copy these two files into this folder.
