#import "@preview/arkheion:0.1.1": arkheion, arkheion-appendices
#import "@preview/mitex:0.2.6": *
#show: arkheion.with(
  title: "Linux中的PATH、可执行文件与符号链接",
  authors: (
    (name: "Geoffrey Xu", email: "13149131068@163.com", affiliation: "Xidian University", orcid: "0009-0006-1640-1812"),
  ),
  // Insert your abstract after the colon, wrapped in brackets.
  // Example: `abstract: [This is my abstract...]`
  //abstract: lorem(55),
  //keywords: ("First keyword", "Second keyword", "etc."),
  date: "March 7, 2026",
)

#set par(spacing: 1.5em)

#set text(
  font: ("Merriweather", "Noto Serif CJK SC"),
  size: 12pt,
  lang: "zh",
) //设置正文字体, Merriweather 是英文使用的字体, Noto Serif CJK SC 是中文使用的字体.

#show heading: set text(font: "New Computer Modern", weight: "bold") //设置标题字体,bold表示粗体

#set heading(numbering: "1.") //设置标题编号格式

#outline(depth: 4) //设置目录深度

// 注释掉 arkheion 模版中的这部分代码
#show heading: it => {
  // H1 and H2
  if it.level == 1 {
    pad(
      bottom: 10pt,
      it,
    )
  } else if it.level == 2 {
    pad(
      bottom: 8pt,
      it,
    )
  } else if it.level >= 3 {
    pad(
      bottom: 6pt,
      it,
    )
  } else {
    it
  }
}

#let titled-block(title: [], body, ..kwargs) = {
  stack(
    dir: ttb,
    spacing: 5pt,
    text(
      size: .9em,
      fill: rgb("#3140e4"),
      sym.triangle.small.stroked.r + sym.space + title,
    ),
    block(
      inset: 10pt,
      width: 100%,
      stroke: 2pt + rgb("#3140e4").lighten(50%),
      ..kwargs.named(),
      body,
    ),
  )
}

#show raw.where(block: true): it => block(
  fill: luma(245),
  inset: 10pt,
  radius: 4pt,
  width: 100%,
  stroke: (left: 3pt + rgb("#3498db")),
  it,
)

#show raw.where(block: false): box.with(
  fill: luma(240),
  inset: (x: 3pt, y: 0pt),
  outset: (y: 3pt),
  radius: 2pt,
)

= Linux 中的 PATH 与可执行文件

== 引入问题

在 Linux 系统中安装 Neovim（nvim）之后，许多初学者会遇到一个看似简单却容易产生误解的问题：究竟需不需要为 nvim 配置环境变量？如果需要，PATH 中应当加入的是 `/bin`、`/usr/bin`，还是 `/usr/bin/nvim` 这样的路径？

要正确回答这个问题，必须从 PATH 的工作机制讲起，而不是从具体软件的安装方式入手。

== PATH 的设计目的与工作方式

PATH 是 shell 用来查找命令的环境变量。它由若干路径组成，每个路径之间用冒号分隔。PATH 的核心设计思想只有一个：PATH 中存放的是“目录”，而不是具体的可执行文件。

当用户在终端中输入一个命令，例如 `nvim`，shell 并不会一次性定位到某个文件，而是按照 PATH 中目录的顺序，依次在这些目录中查找是否存在一个名为 `nvim` 的可执行文件。如果在某个目录中找到了可执行的 `nvim`，查找过程立即停止，并执行该程序。

这一点决定了一个重要结论：向 PATH 中加入目录是正确且规范的行为，而向 PATH 中加入某个具体的可执行文件路径，在语义上就是不正确的。

== 关于 “/usr/bin” 与 “/usr/bin/nvim” 的区别

现在我们可以解释为什么网络上会出现两种看似相似、实则完全不同的说法。

当把 `/usr/bin` 加入 PATH 时，shell 会在该目录中查找 `nvim`，最终执行 `/usr/bin/nvim`。这是 PATH 的标准使用方式，完全符合 POSIX 规范。

而将 `/usr/bin/nvim` 本身加入 PATH，则意味着 PATH 中出现了一个普通文件而非目录。某些 shell 的实现可能在特定情况下仍能执行该文件，但这种行为既不是标准的一部分，也不具备可移植性。因此，这种做法不能被视为正确配置。

在教学和工程实践中，应当明确指出：PATH 的元素必须是目录，这是一条应当被严格遵守的规则。

== 符号链接在命令查找中的角色

在实际系统中，很多用户并没有将 `/usr/bin/nvim` 加入 PATH，却依然能够直接输入 `nvim` 启动编辑器。这并不是因为 PATH 的规则被破坏，而是因为符号链接的存在（符号链接在后面会详细讲解）。

通常情况下，`/usr/bin` 目录本身已经包含在 PATH 中，而 `/usr/bin/nvim` 是该目录下的一个符号链接。这个链接指向真正的 nvim 可执行文件，该文件可能位于任意位置，例如某个用户安装目录或解压后的软件包路径。

当 shell 在 `/usr/bin` 中发现 `nvim` 这个名字时，并不会关心它是普通文件还是符号链接，而是直接执行它。操作系统在执行阶段自动解析符号链接，从而运行真正的程序。

这种机制完全符合 Unix 的设计哲学，也是系统中大量工具采用的通用模式。

== 入口点与实现分离的设计思想

从系统设计角度看，可以将这一模式概括为“入口点与实现分离”。PATH 中的目录提供稳定的入口点，符号链接提供稳定的命令名，而真实的程序实现可以位于任意位置，甚至可以被替换。

这一设计使得系统具备极高的灵活性。例如，在不改变用户使用习惯的前提下，可以随时切换 nvim 的版本，或者将命令重定向到不同的实现。

类似的设计在 Linux 系统中随处可见，例如 Java、Python 以及 Debian 系列发行版中的 alternatives 机制，本质上都遵循同一思想。

== 为什么符号链接优于直接修改 PATH

在课程中有必要强调，相比频繁修改 PATH，使用符号链接更适合系统级工具。

修改 PATH 往往只在交互式 shell 中生效，而 systemd 服务、cron 任务或脚本环境可能拥有不同的 PATH 设置，从而导致“命令在终端能运行，在脚本中却失败”的问题。符号链接则不存在这一问题，因为它依赖的是全局一致的目录结构。

此外，符号链接不会污染用户的环境变量，使系统行为更加可预测，也更符合系统管理的最佳实践。

== 符号链接放置位置的选择

虽然可以在 `/usr/bin` 中手动创建符号链接，但在实际工程中应当注意潜在的风险。系统包管理器在升级或重新安装软件时，可能会覆盖该目录下的文件，从而破坏手动创建的链接。

因此，更推荐的做法是将符号链接放置在 `/usr/local/bin` 中。该目录的用途正是存放本地手动安装的软件入口点，通常已经包含在 PATH 中，同时又不会与系统软件包产生冲突。

== 小结

通过本讲的分析可以得出几个应当牢记的结论。

- PATH 的元素必须是目录，而不是可执行文件
- shell 查找命令时是在 PATH 中的目录内寻找同名可执行文件
- 符号链接是将稳定命令名映射到真实程序的标准方式
- 对于系统级工具，符号链接通常优于修改 PATH
- `/usr/local/bin` 是放置手动创建符号链接的推荐位置

理解这些规则之后，nvim 的 PATH 配置问题不再是一个零散的技巧问题，而是对 Linux 命令查找机制整体理解的一部分。

= 符号链接（Symbolic Link）的原理与具体内容

== 为什么需要符号链接

在 Linux 中，文件系统不仅用于存储数据，也承担着“组织程序入口”的职责。随着系统规模的扩大，程序的真实存放位置、版本数量以及调用方式都会发生变化。如果每一个程序都必须固定在某个目录中，那么系统将难以维护。

符号链接正是为了解决“命令入口稳定，而实现位置可变”这一问题而出现的机制。它允许一个文件名指向另一个路径，从而在不改变用户使用方式的前提下，自由调整程序的真实位置。

== 什么是符号链接

从文件系统的角度看，符号链接本身也是一种文件。它不是目录，也不是普通的可执行文件，而是一种特殊类型的文件，其内容并不是程序指令或用户数据，而是“一段路径字符串”。

这段路径字符串指向另一个文件或目录。操作系统在访问符号链接时，会自动读取这段路径，并将访问请求重定向到目标路径所对应的对象。

因此，可以从概念上这样理解符号链接：它是一个“保存了目标路径的文件”。

== 符号链接的具体内容是什么

要准确理解符号链接，必须澄清一个关键点：符号链接并不包含目标文件的内容，也不复制目标文件的任何数据。

符号链接内部只保存一件东西，即“目标路径”。这个路径可以是绝对路径，也可以是相对路径。例如，一个名为 `nvim` 的符号链接，其内部可能仅保存字符串 `/opt/nvim/bin/nvim`。

当系统尝试执行或读取该符号链接时，会先解析这段路径，再转而访问真正的目标文件。

从这一点可以看出，符号链接本质上是一种路径级别的重定向机制，而不是内容级别的映射。

== 符号链接与普通文件的区别

普通文件的 inode 中记录的是文件的数据块位置，文件名只是 inode 的一个引用。而符号链接的 inode 中记录的并不是数据块，而是目标路径本身。

这意味着，当你删除目标文件时，符号链接并不会随之消失，它仍然存在，只是其内部保存的路径已经无法解析。这种状态下的符号链接通常被称为“悬空链接”或“断链”。

反过来，删除符号链接本身，并不会影响目标文件的存在，因为两者在文件系统中是彼此独立的对象。

== 符号链接在程序执行中的作用过程

当 shell 在 PATH 中的某个目录里找到名为 `nvim` 的文件时，它并不会立即区分这是普通文件还是符号链接，而是尝试执行它。

在执行阶段，内核会检查该文件的类型。如果发现这是一个符号链接，内核会自动读取其内部保存的路径，并跳转到该路径所指向的目标文件，然后再对目标文件进行权限检查和执行。

整个过程对用户和 shell 来说是透明的，这也是为什么符号链接能够无缝地作为命令入口使用。

== 绝对路径链接与相对路径链接

符号链接内部保存的路径可以分为两类：绝对路径和相对路径。

绝对路径符号链接保存的是从根目录开始的完整路径。这种链接的优点是清晰、稳定，不依赖符号链接自身的位置；缺点是在整个目录树被移动时，链接容易失效。

相对路径符号链接保存的是相对于链接所在目录的路径。这种链接在目录整体迁移时更具鲁棒性，但对创建者的路径计算要求更高。

在系统级命令入口中，通常更倾向于使用绝对路径符号链接，以换取行为的确定性。

== 符号链接与硬链接的对比

在理解符号链接时，常常需要将其与硬链接区分开来。两者在外观上相似，但语义完全不同。

- 硬链接是同一个 inode 的多个名字，本质上是同一个文件
- 符号链接是一个独立文件，只是保存了指向另一个路径的字符串
- 删除原文件会影响符号链接，但不会影响硬链接
- 符号链接可以跨文件系统，而硬链接通常不可以

在程序入口和命令重定向场景中，符号链接几乎总是优于硬链接的选择。

== 符号链接在 nvim 场景中的意义

回到 nvim 的例子，将 `/usr/bin/nvim` 或 `/usr/local/bin/nvim` 设为符号链接，其实是在为用户提供一个稳定不变的命令入口。

无论 nvim 的真实可执行文件位于何处，甚至无论未来是否更换版本或实现，只要更新符号链接指向的目标路径，用户输入的 `nvim` 命令就始终保持不变。

这正是 Unix 系统中“工具名稳定、实现可替换”设计哲学的直接体现。

== 小结

符号链接是一种保存了目标路径的特殊文件。它通过路径重定向机制，将文件系统中的一个名字映射到另一个位置。符号链接的存在使得 PATH 可以保持简洁稳定，同时又允许程序的真实位置和版本灵活变化。在理解 PATH、命令查找和系统工具组织方式时，符号链接是一个不可或缺的核心概念。
